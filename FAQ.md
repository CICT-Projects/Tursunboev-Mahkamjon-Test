# ❓ Часто задаваемые вопросы (FAQ)

## 🛠️ Установка и подготовка

### Q: Какую версию .NET нужна?
**A:** Нужна **.NET 10.0** или выше.

Проверить текущую версию:
```bash
dotnet --version
```

Если версия старше → [скачайте новую](https://dotnet.microsoft.com/download)

### Q: Какую версию Node.js нужна?
**A:** Нужна **Node.js 14.0** или выше.

Проверить:
```bash
node --version
npm --version
```

### Q: Обязательно ли использовать localhost?
**A:** Да, в конфигурации жёстко прописано:
- Backend: `http://localhost:5000`
- Frontend: `http://localhost:3000`

Если нужно изменить:
1. В Backend: `Program.cs` измените `urls: "http://localhost:5000"`
2. В Frontend: `App.js` измените `API_BASE_URL`

---

## 🚀 Запуск приложения

### Q: Что делать если Backend не запускается?
**A:** Проверьте:
1. Установлен ли .NET 10.0
2. Порт 5000 не занят
3. Вы находитесь в папке `backend`

Если ничего не помогает:
```bash
dotnet clean
dotnet restore
dotnet run
```

### Q: Почему Frontend не видит Backend?
**A:** CORS ошибка означает:
1. Backend не запущен → запустите его
2. Backend на другом адресе → измените `API_BASE_URL`
3. Неправильный порт → проверьте Console Backend'а

Откройте http://localhost:5000/api/cars в браузере. Если не работает → Backend не запущен.

### Q: Можно ли запустить оба приложения в одном окне?
**A:** Нет. Нужны ДВА отдельных окна:
1. Одно для Backend (`dotnet run`)
2. Одно для Frontend (`npm start`)

Оба должны работать одновременно.

### Q: Что значит "Port 5000 is already in use"?
**A:** Другой процесс уже использует порт 5000.

Решение для Windows PowerShell:
```powershell
Get-Process -Name dotnet | Stop-Process -Force
```

Или закройте старое окно Backend'а.

---

## 💾 База данных

### Q: Где находится база данных?
**A:** По пути:
```
backend\bin\Debug\net10.0\cars.db
```

Это SQLite файл. Можно открыть в SQLite браузере.

### Q: Как очистить всю базу данных?
**A:** Простой способ:
1. Закройте Backend
2. Удалите файл `cars.db`
3. Запустите Backend снова
4. Создастся новая пустая база

### Q: Почему после обновления страницы данные остались?
**A:** Потому что они хранятся в базе данных на сервере, а не в localStorage браузера. Это правильно!

### Q: Как сделать резервную копию данных?
**A:** Просто скопируйте файл `cars.db` в безопасное место.

### Q: Можно ли использовать SQL Server вместо SQLite?
**A:** Да! Измените в `Program.cs`:
```csharp
// Текущий (SQLite):
options.UseSqlite("Data Source=cars.db")

// На SQL Server:
options.UseSqlServer("Server=.;Database=CarsDb;Trusted_Connection=true;")
```

---

## 🎨 Frontend

### Q: Как изменить дизайн?
**A:** Всё в CSS файлах:
- `App.css` - Главные стили
- `styles/*.css` - Стили компонентов

Измените цвета в `background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);`

### Q: Как добавить новый тип запчасти?
**A:** В файле `AddPartForm.js`:
```javascript
const partTypes = [
    'двигатель',
    'кузов',
    'электроника',
    'салон',
    'подвеска',
    'тормоза',
    'трансмиссия',
    'прочее',
    'новый_тип',  // ← Добавьте сюда
];
```

### Q: Как сделать поле обязательным?
**A:** Добавьте `required` к input:
```javascript
<input
  type="text"
  id="name"
  name="name"
  value={formData.name}
  onChange={handleChange}
  required  // ← Это делает поле обязательным
  placeholder="..."
/>
```

### Q: Как увеличить максимальное количество машин на странице?
**A:** По умолчанию нет лимита. Все машины загружаются сразу. Для оптимизации можно добавить пагинацию, но это требует изменений в API.

### Q: Почему иконки emoji не отображаются?
**A:** Они отображаются на всех современных браузерах. Если не видны:
1. Обновите браузер
2. Очистите кэш
3. Попробуйте другой браузер

### Q: Как сделать приложение тёмным режимом?
**A:** Измените цвета в CSS:
```css
.App-header {
  background: #1a1a1a; /* Вместо градиента */
  color: #fff;
}
```

---

## 🔧 Backend (C#)

### Q: Как добавить новое поле в модель Car?
**A:** В файле `Models/Car.cs`:
```csharp
public class Car
{
    public int Id { get; set; }
    // ... остальные поля ...
    public string VIN { get; set; }  // ← Новое поле
}
```

Потом обновите БД (если используете миграции) или просто пересоздайте cars.db.

### Q: Как добавить валидацию?
**A:** В контроллере перед сохранением:
```csharp
if (string.IsNullOrWhiteSpace(car.Name))
{
    return BadRequest("Car name is required");
}
```

### Q: Как логировать запросы?
**A:** В `Program.cs` добавьте:
```csharp
builder.Logging.AddConsole();
```

Потом используйте в контроллере:
```csharp
private readonly ILogger<CarsController> _logger;

_logger.LogInformation($"Adding car: {car.Name}");
```

### Q: Где смотреть Swagger документацию?
**A:** http://localhost:5000/swagger

Там вы сможете тестировать все API endpoints без Postman.

### Q: Как обновить NuGet пакеты?
**A:** 
```bash
dotnet package update
# или для конкретного пакета
dotnet add package Microsoft.EntityFrameworkCore --version 10.0.0
```

### Q: Можно ли использовать Postman для тестирования?
**A:** Да, это очень удобно:
1. Скачайте [Postman](https://www.postman.com/downloads/)
2. Создайте новый запрос
3. Введите URL: `http://localhost:5000/api/cars`
4. Выберите метод (GET, POST, etc.)
5. Отправьте

---

## 🐛 Ошибки и решения

### Q: "Unhandled Rejection (TypeError): Failed to fetch"
**A:** Backend не запущен или неправильный адрес. Проверьте:
```javascript
const API_BASE_URL = 'http://localhost:5000/api';
```

Откройте http://localhost:5000/swagger в браузере.

### Q: "CORS policy: No 'Access-Control-Allow-Origin' header"
**A:** CORS не настроен. Проверьте `Program.cs` содержит:
```csharp
builder.Services.AddCors(options =>
{
    options.AddPolicy("AllowReactApp", policy =>
    {
        policy.WithOrigins("http://localhost:3000")
            .AllowAnyMethod()
            .AllowAnyHeader();
    });
});

app.UseCors("AllowReactApp");
```

### Q: "DbContext is not registered"
**A:** В `Program.cs` должна быть строка:
```csharp
builder.Services.AddDbContext<AppDbContext>(options =>
    options.UseSqlite("Data Source=cars.db"));
```

### Q: "Unable to resolve service for type 'backend.Data.AppDbContext'"
**A:** DbContext не зарегистрирован в Dependency Injection. Добавьте в Program.cs.

### Q: React показывает "Loading..." но данные не загружаются
**A:** 
1. Откройте F12 (Developer Tools) → Console
2. Посмотрите ошибки
3. Убедитесь Backend работает на порту 5000
4. Проверьте сетевой запрос (Network tab)

### Q: Данные сохранились в Backend но не показываются в Frontend
**A:** Возможно, у машины нет Parts. Попробуйте добавить запчасть. Если всё ещё не показывается:
1. Откройте Chrome DevTools → Network
2. Посмотрите, что вернул GET /api/cars
3. Проверьте структуру JSON в консоли

---

## 📈 Расширение функционала

### Q: Как добавить фильтр по марке машины?
**A:** Добавьте состояние в App.js:
```javascript
const [brandFilter, setBrandFilter] = useState('');

const filteredCars = cars.filter(car => 
  car.brand.toLowerCase().includes(brandFilter.toLowerCase())
);
```

Потом отобразите `filteredCars` вместо `cars`.

### Q: Как добавить сортировку по цене?
**A:**
```javascript
const sortedCars = [...cars].sort((a, b) => a.price - b.price);
```

### Q: Как добавить поиск?
**A:**
```javascript
const [searchTerm, setSearchTerm] = useState('');

const searchResults = cars.filter(car =>
  car.name.toLowerCase().includes(searchTerm.toLowerCase())
);
```

### Q: Можно ли добавить экспорт данных в Excel?
**A:** Да, установите пакет:
```bash
npm install xlsx
```

Потом:
```javascript
import * as XLSX from 'xlsx';

const exportToExcel = () => {
  const ws = XLSX.utils.json_to_sheet(cars);
  const wb = XLSX.utils.book_new();
  XLSX.utils.book_append_sheet(wb, ws, "Cars");
  XLSX.writeFile(wb, "cars.xlsx");
};
```

---

## 💡 Оптимизация

### Q: Приложение работает медленно с большим количеством машин
**A:** Добавьте пагинацию в Backend:
```csharp
[HttpGet]
public async Task<ActionResult<IEnumerable<Car>>> GetCars(
    [FromQuery] int pageNumber = 1,
    [FromQuery] int pageSize = 10)
{
    var cars = await _context.Cars
        .Include(c => c.Parts)
        .Skip((pageNumber - 1) * pageSize)
        .Take(pageSize)
        .ToListAsync();
    return Ok(cars);
}
```

### Q: Как кэшировать данные?
**A:** Используйте React.memo или useMemo:
```javascript
const CarList = React.memo(({ cars }) => {
  return <div>...</div>;
});
```

### Q: Как профилировать производительность?
**A:** Используйте Chrome DevTools → Performance tab:
1. Нажмите запись
2. Выполните действия
3. Посмотрите временную шкалу

---

## 🔒 Безопасность

### Q: Нужна ли аутентификация?
**A:** Сейчас нет. Для добавления:
1. Установите `Microsoft.AspNetCore.Identity`
2. Создайте User модель
3. Добавьте [Authorize] к контроллерам

### Q: Данные передаются в открытом виде?
**A:** Да, используется HTTP. Для HTTPS в production:
1. Получите SSL сертификат
2. Настройте HTTPS в Program.cs
3. Обновите URL в Frontend

### Q: Можно ли скрыть API ключи?
**A:** 
- Backend: используйте `appsettings.Development.json` + `appsettings.json`
- Frontend: используйте `.env` файл

Не коммитьте в Git файлы с секретами!

---

## 📚 Обучение и развитие

### Q: Как добавить функцию редактирования?
**A:** Нужно добавить:
1. PUT endpoint в контроллере (уже есть)
2. Form компонент для редактирования
3. API call при submit
4. Обновление state в App.js

### Q: Как добавить загрузку изображений?
**A:** Это требует работы с файлами. Примерно:
1. Добавьте `<input type="file">` в форму
2. Отправьте FormData POST на Backend
3. Сохраните файл на диск
4. Верните URL в React

### Q: Как написать unit тесты?
**A:** 
- Backend: используйте xUnit
- Frontend: используйте Jest (уже установлен)

```javascript
test('renders car name', () => {
  const car = { id: 1, name: 'BMW' };
  // test логика
});
```

### Q: Как задеплоить приложение?
**A:** 
- Backend: Azure, Heroku, VPS
- Frontend: Vercel, Netlify, GitHub Pages
- Database: переместить cars.db или использовать облачную БД

---

## 🆘 Не нашли ответ?

1. Проверьте консоль браузера (F12)
2. Посмотрите логи Backend'а в консоли
3. Попробуйте перезагрузить оба приложения
4. Проверьте сетевые запросы (Network tab)
5. Очистите кэш браузера

Если всё равно не помогает → проверьте что:
- Backend запущен на 5000
- Frontend запущен на 3000
- Установлены правильные версии .NET и Node.js
- Нет опечаток в путях файлов

**Good luck! 🚀**
