# 🚀 Подробная инструкция по запуску

## ✅ Требования

Перед началом убедитесь, что установлены:

### Windows
- **.NET SDK 10.0** или выше → [Скачать](https://dotnet.microsoft.com/download)
- **Node.js 14+** (с npm) → [Скачать](https://nodejs.org/)
- **Git** (опционально) → [Скачать](https://git-scm.com/)

### Проверка версий

```bash
# Проверить версию .NET
dotnet --version

# Проверить версию Node.js и npm
node --version
npm --version
```

---

## 📂 Структура папок

```
C:\Users\[ВашИмя]\Desktop\HomeWork\
├── backend\              ← Backend (C# ASP.NET Core)
├── frontend\             ← Frontend (React)
├── README.md             ← Главная документация
└── API_EXAMPLES.md       ← Примеры запросов
```

---

## 🔧 Шаг 1: Запуск Backend (C# ASP.NET Core)

### 1.1 Откройте PowerShell или CMD

Перейдите в папку `backend`:

```powershell
cd C:\Users\[ВашИмя]\Desktop\HomeWork\backend
```

### 1.2 Восстановите NuGet пакеты

```bash
dotnet restore
```

Это скачает все необходимые зависимости.

### 1.3 Запустите Backend

```bash
dotnet run
```

#### Ожидаемый вывод:

```
info: Microsoft.AspNetCore.Hosting.Diagnostics[1]
      Request starting HTTP/1.1 GET http://localhost:5000/swagger/index.html ...
info: Microsoft.EntityFrameworkCore.Database.Command[20101]
      Executed DbCommand (0ms) [Parameters=] ...
Application started. Press Ctrl+C to exit.
```

### 1.4 Проверьте, что Backend работает

Откройте в браузере:
- **API**: http://localhost:5000/api/cars
- **Swagger документация**: http://localhost:5000/swagger

Должны увидеть пустой JSON массив `[]` или Swagger UI.

### ✅ Backend готов к работе!

**Оставьте это окно открытым!** Backend должен работать в фоне.

---

## 🎨 Шаг 2: Запуск Frontend (React)

### 2.1 Откройте новое окно PowerShell/CMD

Перейдите в папку `frontend`:

```powershell
cd C:\Users\[ВашИмя]\Desktop\HomeWork\frontend
```

### 2.2 Установите зависимости

```bash
npm install
```

Это может занять 2-5 минут. Будет скачано много пакетов.

### 2.3 Запустите React приложение

```bash
npm start
```

#### Ожидаемый вывод:

```
webpack compiled successfully
Compiled successfully!

You can now view frontend in the browser.

  Local:            http://localhost:3000
```

Браузер автоматически откроется на http://localhost:3000

### ✅ Frontend готов!

---

## 🎯 Шаг 3: Тестирование приложения

### Проверка 1: Добавить автомобиль

1. На странице http://localhost:3000 вы увидите форму "Добавить новый автомобиль"
2. Заполните форму:
   - **Название машины**: `BMW X5`
   - **Марка**: `BMW`
   - **Год выпуска**: `2023`
   - **Цена**: `5000000`
3. Нажмите кнопку **"➕ Добавить автомобиль"**

✅ Машина появилась в списке ниже!

### Проверка 2: Добавить запчасть

1. Найдите добавленную машину в списке
2. Нажмите **"➕ Добавить запчасть"**
3. Заполните форму:
   - **Название запчасти**: `Масло моторное`
   - **Цена**: `5000`
   - **Тип запчасти**: `двигатель`
4. Нажмите **"✅ Добавить"**

✅ Запчасть появилась под машиной!

### Проверка 3: Обновить страницу

1. Нажмите **F5** или **Ctrl+R** чтобы обновить страницу
2. Подождите загрузки

✅ **Все машины и запчасти остались!** Данные сохранены в базе!

### Проверка 4: Удаление

1. Нажмите **❌** рядом с запчастью → запчасть удалится
2. Нажмите **🗑️** рядом с названием машины → машина удалится

---

## 📊 Примеры данных для тестирования

### Машина 1
```
Название: Mercedes-Benz E-Class
Марка: Mercedes
Год: 2024
Цена: 4500000
```

Запчасти:
- Тормозные колодки (8000, тормоза)
- Воздушный фильтр (2500, двигатель)

### Машина 2
```
Название: Audi A4
Марка: Audi
Год: 2023
Цена: 3800000
```

Запчасти:
- Свечи зажигания (4000, двигатель)
- Амортизаторы (15000, подвеска)

---

## 🐛 Решение проблем

### ❌ Проблема: "Backend не запускается"

**Решение 1**: Проверьте версию .NET
```bash
dotnet --version
```
Должно быть 10.0 или выше.

**Решение 2**: Очистите кэш и переустановите пакеты
```bash
cd backend
dotnet clean
dotnet restore
dotnet run
```

### ❌ Проблема: "CORS ошибка в браузере"

Это означает, что Backend не запущен на порту 5000.

**Решение**:
1. Откройте http://localhost:5000/api/cars в браузере
2. Если не работает → Backend не запущен
3. Запустите Backend: `dotnet run` в папке `backend`

### ❌ Проблема: "Port 5000 уже в использовании"

Backend не закрылся после предыдущего запуска.

**Решение**:
```powershell
# Windows PowerShell (администратор)
Get-Process -Name dotnet | Stop-Process -Force

# Или
netstat -ano | findstr :5000
taskkill /PID [PID] /F
```

### ❌ Проблема: "Данные не сохраняются"

Проверьте, что Backend работает и есть сообщение в консоли Backend про сохранение данных.

**Решение**:
1. Откройте консоль Backend (где запущен `dotnet run`)
2. Добавьте машину в React
3. Должны увидеть сообщения про запросы к API в консоли Backend

### ❌ Проблема: "npm install не работает"

```bash
# Очистите npm кэш
npm cache clean --force

# Удалите папку node_modules
rmdir /s node_modules

# Переустановите
npm install
```

### ❌ Проблема: "React не загружается"

Port 3000 может быть занят.

**Решение**:
```bash
# Запустите на другом порту
npm start -- --port 3001
```

Тогда откройте http://localhost:3001

---

## 🔌 Проверка API вручную

Откройте **PowerShell** и выполните:

### Добавить машину
```powershell
$body = @{
    name = "Test Car"
    brand = "Test"
    year = 2023
    price = 1000000
} | ConvertTo-Json

Invoke-RestMethod -Uri "http://localhost:5000/api/cars" `
  -Method POST `
  -Headers @{"Content-Type"="application/json"} `
  -Body $body
```

### Получить все машины
```powershell
Invoke-RestMethod -Uri "http://localhost:5000/api/cars"
```

---

## 📁 Где находится база данных?

SQLite база данных находится по пути:
```
backend\bin\Debug\net10.0\cars.db
```

Эта папка создаётся автоматически при первом запуске Backend.

### Для очистки данных:

Просто удалите файл `cars.db` и перезапустите Backend. Он создаст новую пустую базу.

---

## 📝 Полезные команды

### Backend

```bash
# Запустить в режиме разработки
dotnet run

# Запустить с горячей перезагрузкой
dotnet watch run

# Просмотреть все миграции
dotnet ef migrations list

# Создать новую миграцию
dotnet ef migrations add MigrationName

# Применить миграцию
dotnet ef database update

# Очистить всё
dotnet clean
```

### Frontend

```bash
# Запустить
npm start

# Собрать для production
npm run build

# Запустить тесты
npm test

# Очистить кэш
npm cache clean --force
```

---

## 🎓 Что это всё означает?

- **Backend** (C#) → Сервер, обрабатывает запросы, сохраняет в БД
- **Frontend** (React) → Веб-приложение, которое вы видите в браузере
- **API** → Способ общения между Frontend и Backend
- **CORS** → Разрешение для Frontend общаться с Backend
- **SQLite** → Простая база данных, хранит все данные
- **Entity Framework** → Инструмент для работы с БД в C#

---

## 🎉 Готово!

Если всё работает:
- ✅ Frontend открывается на http://localhost:3000
- ✅ Backend работает на http://localhost:5000
- ✅ Машины добавляются и сохраняются
- ✅ Запчасти добавляются и сохраняются
- ✅ После обновления страницы всё остаётся

**Поздравляем! Ваше приложение полностью работает! 🚀**

---

## 📞 Нужна помощь?

Проверьте:
1. Оба окна (Backend и Frontend) открыты и работают
2. Порты 5000 и 3000 не заняты
3. Установлены нужные версии .NET и Node.js
4. Нет CORS ошибок в консоли браузера (F12)
5. В консоли Backend видны запросы от Frontend

Если всё проверили и не работает → попробуйте перезагрузить оба приложения.

**Happy Coding! 🎉**
