# 🚗 Платформа для управления автомобилями

Полноценное веб-приложение для добавления и управления автомобилями и их запчастями.

## 📋 Требования

- **Frontend**: React (CRA)
- **Backend**: C# ASP.NET Core Web API
- **Database**: SQLite
- **Node.js**: версия 14+
- **.NET SDK**: версия 10.0+

## 📁 Структура проекта

```
HomeWork/
├── backend/                    # C# Backend
│   ├── Models/
│   │   ├── Car.cs             # Модель автомобиля
│   │   └── Part.cs            # Модель запчасти
│   ├── Data/
│   │   └── AppDbContext.cs    # Entity Framework DbContext
│   ├── Controllers/
│   │   ├── CarsController.cs  # API для автомобилей
│   │   └── PartsController.cs # API для запчастей
│   ├── Program.cs             # Конфигурация приложения
│   ├── backend.csproj         # Проект файл
│   ├── cars.db                # SQLite база данных
│   └── appsettings.json       # Настройки
│
└── frontend/                   # React приложение
    ├── src/
    │   ├── components/
    │   │   ├── AddCarForm.js      # Форма добавления машины
    │   │   ├── AddPartForm.js     # Форма добавления запчасти
    │   │   ├── CarCard.js         # Карточка машины
    │   │   └── CarList.js         # Список машин
    │   ├── styles/
    │   │   ├── AddCarForm.css
    │   │   ├── AddPartForm.css
    │   │   ├── CarCard.css
    │   │   └── CarList.css
    │   ├── App.js              # Главный компонент
    │   ├── App.css
    │   └── index.js
    ├── package.json
    └── public/
        └── index.html
```

## 🚀 Как запустить проект

### 1️⃣ Предварительная подготовка

Убедитесь, что установлены:
- .NET SDK 10.0 или выше: [Download](https://dotnet.microsoft.com/download)
- Node.js 14+: [Download](https://nodejs.org/)

### 2️⃣ Запуск Backend

```bash
# Перейти в папку backend
cd backend

# Восстановить зависимости
dotnet restore

# Создать миграции базы данных (если нужно)
dotnet ef database update

# Запустить приложение
dotnet run
```

Backend будет доступен на: **http://localhost:5000**

Документация API доступна на: **http://localhost:5000/swagger**

### 3️⃣ Запуск Frontend

```bash
# Перейти в папку frontend
cd frontend

# Установить зависимости
npm install

# Запустить приложение
npm start
```

Frontend автоматически откроется на: **http://localhost:3000**

## 🔌 API Endpoints

### Автомобили

#### 📥 Получить все автомобили
```
GET /api/cars
```

**Ответ:**
```json
[
  {
    "id": 1,
    "name": "BMW X5",
    "brand": "BMW",
    "year": 2023,
    "price": 5000000,
    "createdAt": "2024-12-29T10:30:00Z",
    "parts": [
      {
        "id": 1,
        "name": "Масло моторное",
        "price": 5000,
        "type": "двигатель",
        "carId": 1,
        "createdAt": "2024-12-29T10:35:00Z"
      }
    ]
  }
]
```

#### ➕ Добавить новый автомобиль
```
POST /api/cars
Content-Type: application/json

{
  "name": "BMW X5",
  "brand": "BMW",
  "year": 2023,
  "price": 5000000
}
```

**Ответ:**
```json
{
  "id": 1,
  "name": "BMW X5",
  "brand": "BMW",
  "year": 2023,
  "price": 5000000,
  "createdAt": "2024-12-29T10:30:00Z",
  "parts": []
}
```

#### 📖 Получить автомобиль по ID
```
GET /api/cars/{id}
```

#### ✏️ Обновить автомобиль
```
PUT /api/cars/{id}
Content-Type: application/json

{
  "id": 1,
  "name": "BMW X7",
  "brand": "BMW",
  "year": 2024,
  "price": 6000000
}
```

#### 🗑️ Удалить автомобиль
```
DELETE /api/cars/{id}
```

### Запчасти

#### 📥 Получить все запчасти
```
GET /api/parts
```

#### 📥 Получить запчасти конкретной машины
```
GET /api/parts/car/{carId}
```

**Ответ:**
```json
[
  {
    "id": 1,
    "name": "Масло моторное",
    "price": 5000,
    "type": "двигатель",
    "carId": 1,
    "createdAt": "2024-12-29T10:35:00Z"
  }
]
```

#### ➕ Добавить запчасть
```
POST /api/parts
Content-Type: application/json

{
  "name": "Масло моторное",
  "price": 5000,
  "type": "двигатель",
  "carId": 1
}
```

#### ✏️ Обновить запчасть
```
PUT /api/parts/{id}
Content-Type: application/json

{
  "id": 1,
  "name": "Масло моторное синтетическое",
  "price": 7000,
  "type": "двигатель",
  "carId": 1
}
```

#### 🗑️ Удалить запчасть
```
DELETE /api/parts/{id}
```

## 🎯 Функциональность

### Frontend
- ✅ Добавление новых автомобилей через форму
- ✅ Отображение списка всех автомобилей в красивых карточках
- ✅ Добавление запчастей к каждому автомобилю
- ✅ Отображение запчастей с разбиением по типам
- ✅ Удаление автомобилей и запчастей
- ✅ Расчёт общей стоимости запчастей
- ✅ Красивый и интуитивный UI с градиентами

### Backend
- ✅ REST API для CRUD операций
- ✅ Связь One-to-Many между Car и Part
- ✅ Использование Entity Framework Core
- ✅ SQLite база данных
- ✅ CORS настроен для React приложения
- ✅ Swagger документация

### Database
- ✅ Автоматическое создание таблиц при первом запуске
- ✅ Каскадное удаление запчастей при удалении машины
- ✅ Индексирование по внешним ключам

## 🔐 Модели данных

### Car
```csharp
public class Car
{
    public int Id { get; set; }
    public string Name { get; set; }          // Название машины
    public string Brand { get; set; }         // Марка
    public int Year { get; set; }             // Год выпуска
    public decimal Price { get; set; }        // Цена
    public DateTime CreatedAt { get; set; }   // Дата создания
    public ICollection<Part> Parts { get; set; } // Запчасти
}
```

### Part
```csharp
public class Part
{
    public int Id { get; set; }
    public string Name { get; set; }         // Название запчасти
    public decimal Price { get; set; }       // Цена
    public string Type { get; set; }         // Тип запчасти
    public DateTime CreatedAt { get; set; }  // Дата создания
    public int CarId { get; set; }           // ID машины
    public Car Car { get; set; }             // Ссылка на машину
}
```

## 🧪 Пример использования

### 1. Добавить автомобиль
```bash
curl -X POST http://localhost:5000/api/cars \
  -H "Content-Type: application/json" \
  -d '{
    "name": "Toyota Camry",
    "brand": "Toyota",
    "year": 2023,
    "price": 3000000
  }'
```

### 2. Получить все автомобили
```bash
curl http://localhost:5000/api/cars
```

### 3. Добавить запчасть к машине (ID = 1)
```bash
curl -X POST http://localhost:5000/api/parts \
  -H "Content-Type: application/json" \
  -d '{
    "name": "Фильтр воздушный",
    "price": 3500,
    "type": "двигатель",
    "carId": 1
  }'
```

### 4. Получить запчасти машины
```bash
curl http://localhost:5000/api/parts/car/1
```

### 5. Удалить запчасть
```bash
curl -X DELETE http://localhost:5000/api/parts/1
```

## 🎨 Особенности UI

- 🎨 **Современный дизайн** с использованием градиентов
- 📱 **Адаптивный** для мобильных устройств
- 🎯 **Интуитивный** интерфейс
- ⚡ **Быстрая** загрузка и отклик
- 🔄 **Реал-тайм** обновление данных

## 📊 Типы запчастей

- двигатель
- кузов
- электроника
- салон
- подвеска
- тормоза
- трансмиссия
- прочее

## 🔍 Troubleshooting

### Backend не запускается
```bash
# Очистить кэш NuGet
dotnet nuget locals all --clear

# Восстановить заново
dotnet restore
```

### CORS ошибка
Убедитесь, что Backend запущен на `http://localhost:5000` а Frontend на `http://localhost:3000`.

### База данных не создаётся
Backend автоматически создаст `cars.db` в папке `bin/Debug/net10.0/` при первом запуске.

### Port 3000 или 5000 уже занят
```bash
# Изменить port для frontend в .env
# Или использовать другой port
npm start -- --port 3001
```

## 📝 Лицензия

MIT License

## 👨‍💻 Автор

Создано как учебный проект для демонстрации полного стека разработки (Full-Stack).

---

**Наслаждайтесь разработкой! 🚀**
